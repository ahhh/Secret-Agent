# URL output from `sam deploy` — ApiUrl output value.
# Set this as a GitHub secret named PUBLIC_API_URL for the deploy workflow.
PUBLIC_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com
