import { defineConfig } from 'astro/config';

export default defineConfig({
  output: 'static',
  // Update site to your GitHub Pages URL and base to your repo name.
  // e.g. site: 'https://youruser.github.io', base: '/secret-agent'
  site: 'https://yourusername.github.io',
  base: '/secret-agent',
});
